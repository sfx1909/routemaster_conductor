import 'package:flutter/material.dart';

class UnguardedPage extends StatelessWidget {
  const UnguardedPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('UnguardedPage'),
      ),
    );
  }
}
